# OPL Libraries

Include in this folder the appropriate OPL jar (oplall.jar) from your 
OPL installation directory. 

In Windows, make sure the associated DLL (oplall.dll) is also included.

Specific instructions for Mac OS users are provided in those classes that 
require specific settings of -Djava.library.path and environment variable
DYLD_LIBRARY_PATH